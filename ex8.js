/*Crie uma função chamada menorNumero 
que recebe três números como parâmetros e retorna o menor deles.*/
function menorNumero(a,b,c){
    if(a<b || a<c){
        return a
    }else if (b < A || b < c){
        return b
    }else{
        return c 
    }
}
console.log (menorNumero(a=1, b=3, c=6))